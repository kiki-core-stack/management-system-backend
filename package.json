{
  "type": "module",
  "private": true,
  "scripts": {
    "build": "NODE_ENV=production bun run build:exec --outdir=./dist",
    "build:exec": "bun run ./src/core/generate-production-routes.ts && bun --env-file=./.env.production.local build ./src/index.ts --minify --target bun",
    "compile": "NODE_ENV=production bun run build:exec --compile --outfile ./dist/index",
    "dev": "NODE_ENV=development bun --env-file=./.env.development.local --watch run ./src/index.ts",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "type-check": "tsc"
  },
  "dependencies": {
    "@asteasolutions/zod-to-openapi": "^7.2.0",
    "@hono/swagger-ui": "^0.4.1",
    "@hono/zod-validator": "^0.4.1",
    "@kikiutils/kiki-core-stack-pack": "^0.42.1",
    "@kikiutils/mongoose": "^2.1.1",
    "@scalar/hono-api-reference": "^0.5.159",
    "consola": "^3.2.3",
    "defu": "^6.1.4",
    "glob": "^11.0.0",
    "hono": "^4.6.10",
    "node-ciphers": "^1.1.5",
    "on-change": "^5.0.1",
    "pino": "^9.5.0",
    "pino-pretty": "^13.0.0"
  },
  "devDependencies": {
    "@kikiutils/eslint-config": "^0.4.0",
    "@kikiutils/tsconfigs": "^3.0.2",
    "@types/bun": "^1.1.13",
    "typescript": "^5.6.3"
  }
}
